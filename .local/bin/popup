#!/usr/bin/env bash

TITLE=""
TEXT=""
while [[ $# -gt 0 ]]; do
  case "$1" in
    --title)
      TITLE="$2"
      shift 2
      ;;
    *)
      TEXT+="${TEXT:+ }$1"
      shift
      ;;
  esac
done

echo "$TEXT"

zenity --text-info --title="$TITLE" <<<"$TEXT"
