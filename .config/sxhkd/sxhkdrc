super + Return
  alacritty

super + F{2,3,4,5}
  {chromium,code,pcmanfm,terminal -e mus}

super + shift + control + alt + {2,3,4,5}
  {chromium,code,pcmanfm,terminal -e mus}

super + v
  copyq show

super + ctrl + {x,m,c,b,n}
  {xkill, pavucontrol, colorpicker --short --one-shot | xsel -b, book, notion-app; notion-calendar-electron}

super + shift + {space,s,a,b,c,d,p,g,o,w,y,j}
  {rofi -show drun, power-menu, auto-menu,changelog-menu,c-menu,s-menu,p-menu, github-menu,open-menu, wifi-menu,youtube-menu,dl-menu}

super + shift + ctrl + alt + {space,s,a,b,c,d,p,g,o,w,y}
  {rofi -show drun, powermenu, auto-menu,changelog-menu,cmenu,smenu,pmenu, github-menu,open-menu, wifimenu,youtube-menu}

super + shift + t
  flyterm

XF86Audio{LowerVolume,RaiseVolume}
  pactl set-sink-volume @DEFAULT_SINK@ {-10%, +10%}

XF86AudioMute
  pactl set-sink-mute @DEFAULT_SINK@ toggle

XF86MonBrightness{Down,Up}
  light {-U,-A} 5

super + control + p
  flameshot gui
